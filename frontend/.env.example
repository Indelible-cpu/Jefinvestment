# =============================================================================
# JIMS ERP — Frontend Environment Variables EXAMPLE
# Copy this to .env.local and fill in real values.
# NEVER commit .env.local to Git.
# =============================================================================

# ── Backend API URL (this is intentionally public — it is not a secret) ───────
# Point to your Cloudflare Worker URL or custom domain
VITE_API_URL=https://jef-erp-backend.YOUR_SUBDOMAIN.workers.dev
